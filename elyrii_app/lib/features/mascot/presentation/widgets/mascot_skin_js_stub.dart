/// Stub pour les plateformes non-web (iOS, Android).
/// Sur ces plateformes, l'injection JS n'est pas disponible, donc on ne fait rien.
void evalJs(String script) {
  // No-op on non-web platforms
}
